# Architect + Arena native workflow acceptance

2026-09-22, Windows Codex. Isolated greenfield asynchronous cache fixture; this adds no production cache feature to PStack.

Four native independent runners produced caller-first usage, typed unimplemented sketches, module maps and rationales. Three ran concurrently and the fourth ran in a second wave within available capacity. Each received the same task and shipped runner template, used a separate directory, and did not receive the private picker rubric. Parent read all 19 candidate files, retained their hashes, and screened architectural red flags. Two actual whole-shape designs emerged: immutable registry-entry replacement and operation-owned mutable cells.

An independent same-model cross-judge read all candidates and the rubric after runners completed. Both parent and judge selected candidate 3. The judge caught a callback-placement concern in candidate 1 missed by the initial parent assessment; the parent accepted that correction and retained the original candidate as rejected fallback evidence. SYNTHESIS.md records the base, grafts, rejected shapes and no dropouts.

The selected typed sketch is preserved as selected-sketch.ts. A matching executable scaffold produced 11 failing behavior checks; filling in the chosen shape produced 11 passing checks. Coverage includes pending promise identity, independent keys, completion-based TTL/equality, undefined, sync and async failures/retry, reentrancy, and old success/failure both before and after newer settlement. A separate non-author review reran all 11 checks and found no mismatch to the stated contract.

No implementation contract deviation or repeated structural workaround arose; the conditional scrap/redesign branch was assessed but not exercised. Native model inheritance, not model-family diversity. Directory isolation, not a worktree test. No production integration, UI, external service or TypeScript compilation claimed. Clock contract and self-await exclusions are explicit. Parent and judge reports retain their different initial assessments rather than retroactively claiming agreement on every detail.

Reproduce from this directory: `node --test cache.test.mjs`.
